{{#each markerWrappers as |wrapper|}}
	{{collection-marker model=wrapper.item
	isExpanded=wrapper.isExpanded isClicked=wrapper.isClicked
	minimizeAllAction=(action 'minimizeAllMarkers')
	markerColor=markerColor}}
{{/each}}